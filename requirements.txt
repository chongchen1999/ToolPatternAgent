colorama==0.4.6           # For color formatting in the terminal output
python-dotenv==1.0.0      # For loading environment variables from a .env file
groq==0.1.0               # Assumed library for interacting with the Groq client API (replace with actual version if known)